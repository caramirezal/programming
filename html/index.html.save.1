<!DOCTYPE html>

<html>
	<head>
		<style>
			div.leftPanel {
				height:100vh;
				width:20%;
				float:left;
				padding:20px;
				font-size:25px;
				font.family:arial;
				background-color:white;

			}
			div.rigthSection {
				overflow:hidden;
				height:100vh;
				font-size:30px;
				padding-top:0px;
				padding-left:100px;
				padding-right:100px;
			}
			ol.leftPanel li {
				color:black;
				margin:5px;
				padding:5px;
				list-style-type:decimal;

			}
			ol {
    				counter-reset: item;
			}

			ol > li {
    				counter-increment: item;
			}

			ol ol > li {
    				display: block;
			}

			ol ol > li:before {
    				content: counters(item, ".") ". ";
    				margin-left: -20px;
			}
   OL { counter-reset: item }
   LI { display: block }
   LI:before { content: counters(item, ".") " "; counter-increment: item }			h1 {
				font-size:40px;
			}
		</style>
	</head>

	<body>

		<div class="leftPanel"> 
			<ol class="leftPanel">
				<li>SQUAD</li>
				<li>Overview</li>
				<li>Installation</li>
				<li>Loading a network model</li>
					<ol> 
						<li>SQUAD format</li>
						<li>Starting from a discrete model</li>
						<li>Starting from a connectivity matrix</li>	
					</ol>

				<li>Running simulations</li>
					<ol>
						<li>Setting initial conditions</li>
						<li>Setting parameters</li>
						<li>Simulating perturbations</li>
						<li>Simulating mutants</li>
						<li>Normalized Hill Cubes</li> 
					</ol>
				<li>Calculating Steady States</li>
			</ol>
		</div>

		<div class="rigthSection">

	
<h1 > Standardized Qualitative Analysis of Dynamics (squad) R package </h1>

<p> During the definition of a regulatory network model it is frequently convenient to start 
with simple formalisms and then to transit to more refined models. Continuous interpolations 
of Boolean Regulatory Networks (BRN) are defined so as to preserve dynamical characteristics 
of discrete model but extending the range of values of the variables. In this tutorial we 
presented the main features of the squad R package to simulate continuous interpolations of 
BRN models.   </p>

<p> squad R package is a useful tool to define, set up and run simulations using continuous 
interpolations of regulatory network models. It also can perform stationary states search. </p>

<h2> 1. Overview of squad R package </h2>

<p> The next figure shows schematically the main features of the squad R package. squad 
extends the functionality of the BoolNet R package. BoolNet has integrated an optimized 
method to find synchronous stable states by using the Dubrova algorithm. By the other hand, 
in an asynchronous context SQUAD performs an optimized heuristic method using BDD 
representations and model checking. Hence, the modeller can start defining a discrete BRN 
model and choose synchronous or asynchronous efficient methods for stationary states search.
</p>

<p>Additionally, if needed the modeler can transit to more refined continuous methods as the 
squad or normalized Hill Cubes interpolations of BRN models. </p>


			</p>
		</div>
	</body>
</html>
